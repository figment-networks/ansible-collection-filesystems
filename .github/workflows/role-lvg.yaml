name: Run pull-request molecule job for lvg
on:
  pull_request:
    paths:
    - 'roles/lvg/*'
    - 'roles/lvg/defaults/*'
    - 'roles/lvg/meta/*'
    - 'roles/lvg/tasks/*'
jobs:
  molecule:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        scenario:
        - 1drive
    steps:
    - name: Check out the repo
      # versioneer: updater=github repository=actions/checkout max-major=7 tags=github
      uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
    - name: Set up Python
      # versioneer: updater=github repository=actions/setup-python max-major=7 tags=github
      uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
    - name: Install ansible-core
      run: |
        python -m pip install --upgrade pip wheel
        python -m pip install --upgrade ansible-core molecule
    - name: Install current collection
      run: ansible-galaxy collection install . --upgrade
    - name: Run molecule
      run: |
        cd roles/lvg
        molecule --debug --base-config ../../molecule/molecule-base.yml test --scenario-name ${{ matrix.scenario }}
